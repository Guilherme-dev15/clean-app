rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ATENÇÃO: Esta regra permite acesso total à sua base de dados.
    // É segura para o emulador local, mas NUNCA deve ser usada em produção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}